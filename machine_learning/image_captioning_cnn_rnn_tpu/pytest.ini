[pytest]
# The test modules import top-level project modules (config, data_preprocessing).
# `pythonpath = .` puts the project root on sys.path during collection, which is
# the documented pytest-7+ way to make those imports resolve in CI regardless of
# the invoking working directory. (Supersedes per-file sys.path hacks.)
pythonpath = .
testpaths = tests
